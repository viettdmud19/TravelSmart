

<div style="background-color: #3E4999">
    <p style="text-align:right; color:white;margin: 10px 30px">HOTLINE: 19005678 - Hệ thống cửa hàng uy tín</p>
</div>
<div class="header">
<div class="container">
    <div class="navbar-header">
        <img src="~/Images/logo.png" />
    </div>

    <ul class="nav navbar-nav navbar-right">
         @Html.Action("GioHangPartial", "GioHang")
        @Html.Action("LoginLogout","Travel")
    </ul>
</div>
    </div>
<nav class="navbar navbar-inverse">
    <!--navbar-fixed-top-->
    <div class="container">

        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar1" aria-expanded="false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar1">
            <ul class="nav navbar-nav">
                <li ><a href="@Url.Action("Index","Travel")">TRANG CHỦ</a> </li>
                <li><a href="@Url.Action("Gioithieu","Travel")">GIỚI THIỆU</a> </li>
                <li><a href="@Url.Action("Index_TT","TinTuc")">TIN TỨC</a> </li>
                <li><a href="@Url.Action("LienKet","Travel")">TÌM GIAN KẾT NỐI</a></li>
            </ul>
        


            @using (Html.BeginForm("Search", "Search", FormMethod.Get, new { @class = "navbar-form navbar-right", @role = "search" }))
            {
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Tìm kiếm" name="strSearch">
                </div>
                <button type="submit" class="btn btn-default">Tìm</button>
            }



        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-->
</nav>
